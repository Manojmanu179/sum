

#include <stdio.h>
int sum(int num);
int main() 
{
 int n;
  printf("Enter the number till where the sum is needed\n");
  scanf("%d",&n);
  printf ("The sum till %d is = %d",n,sum(n));
    return 0;
}
 int sum(int num)
 { 
  int a=0;
     for(int i=num;i>0;i--)
      a+=i;
  return a;
}
